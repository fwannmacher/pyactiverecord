"""
Generated by <Python Project Utils>
Visit the project in http://code.google.com/p/python-project-utils/
"""

import unittest
import active_record.util

class PluralizerTestCase(unittest.TestCase):
	def test_pluralize(self):
		self.assertEqual(active_record.util.Pluralizer.pluralize("life"), "lives")
		self.assertEqual(active_record.util.Pluralizer.pluralize("play"), "plays")
		self.assertEqual(active_record.util.Pluralizer.pluralize("study"), "studies")
		self.assertEqual(active_record.util.Pluralizer.pluralize("attach"), "attaches")
		self.assertEqual(active_record.util.Pluralizer.pluralize("wash"), "washes")
		self.assertEqual(active_record.util.Pluralizer.pluralize("pass"), "passes")
		self.assertEqual(active_record.util.Pluralizer.pluralize("radius"), "radii")
		self.assertEqual(active_record.util.Pluralizer.pluralize("s"), "ss")
		self.assertEqual(active_record.util.Pluralizer.pluralize("alias"), "aliases")
